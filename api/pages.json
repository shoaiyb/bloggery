---
layout: null
---
{% for page in site.pages %}
{
  "{{ page.title }}": {
    "title": "{{ page.title }}",
    "content": "{{ page.content }}",
    "date": "{{ page.date }}",
    "categories": {{ page.categories }},
    "tags": {{ page.tags }},
    "url": "{{ page.url }}",
    "id": "{{ page.id }}"
}
{% endfor %}
